import React from 'react';

const FoodCard = ({item}) => {
    const { name, image, price, recipe, _id } = item;
    return (
        <div>
            
        </div>
    );
};

export default FoodCard;